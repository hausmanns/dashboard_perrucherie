.venv/
__pycache__/
*.pyc
.DS_Store
.env

# NOTE: data/dashboard.db is intentionally NOT ignored.
# The SQLite database travels with the repo so a `git pull`
# on another machine brings your data along.
